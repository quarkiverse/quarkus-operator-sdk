name: Build against latest JOSDK version

on:
  push:
    branches: [ next ]
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      quarkus-pr:
        description: 'Quarkus PR number to use to run a QOSDK build with, takes priority over version'
        type: string
        required: false

jobs:
  build-with-latest-josdk-snapshot:
    uses: ./.github/workflows/build-for-quarkus-version.yml
    with:
      quarkus-version: '999-SNAPSHOT'
      quarkus-pr: ${{inputs.quarkus-pr}}
      java-version: 25
      branch: "next"
